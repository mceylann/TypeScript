const user = {name:"Mehmet", surname:"Ceylan", age:15};

let newName: string = "Ahmet";

console.log(user.name);
console.log(newName);


// tsc to compile into .js file

/**
 * TYPES:
 * number
 * string
 * boolean
 * null/undefined/void*
 * object
 * array
 * tuples
 * ...
 * any / unknown / never
 * 
 * all types are lowercase !
 * 
 */